spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/zappqui_auth
    username: zappqui
    password: zappqui123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    baseline-on-migrate: true
    locations: classpath:db/migration
  redis:
    host: localhost
    port: 6379

server:
  port: 8081